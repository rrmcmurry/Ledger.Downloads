- Fixed a startup failure that could occur after an install or update wrote the demo ledger's recent-file timestamp in an older Windows format. Ledger now reads and safely migrates that timestamp format, and future installers write the standard format directly.
